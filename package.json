{
  "name": "rai-ux",
  "devDependencies": {
    "webpack": "5.9.0",
    "webpack-cli": "4.2.0"
  },
  "engines": {
    "node": "14.15.5"
  },
  "scripts": {
    "build": "cd $APP_BASE && npm install && npm run build && cd ..",
    "start": "cd $APP_BASE && npm start && cd .."
  }
}